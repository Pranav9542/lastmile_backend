import { Router } from 'express'; import { authenticate,allow } from '../middleware/auth.js'; import { createOrder,getOrder,listOrders,quote,reschedule } from '../controllers/order.controller.js'; const r=Router();r.use(authenticate);r.post('/quote',allow('CUSTOMER','ADMIN'),quote);r.post('/',allow('CUSTOMER'),createOrder);r.get('/',allow('CUSTOMER'),listOrders);r.get('/:id',getOrder);r.get('/:id/tracking',getOrder);r.post('/:id/reschedule',allow('CUSTOMER'),reschedule);export default r;
